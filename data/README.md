# Data

Runtime-generated application data: SQLite database files, vector store indexes, cached memory state.

This entire folder is **gitignored**. It is created and populated at runtime by the `database/` and `memory/` modules — nothing here should ever be committed.
