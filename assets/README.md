# Assets

Static, non-code resources bundled with the assistant:
- `sounds/` — notification/earcon sounds (wake acknowledgement, error tone, etc.)
- `icons/` — system tray / dashboard icons
- `images/` — images used in the dashboard UI

These are committed to source control (unlike `models/`, which is gitignored) since they're small, static, and versioned with the app.
