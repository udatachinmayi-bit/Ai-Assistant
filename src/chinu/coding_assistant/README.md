# Coding Assistant

Isolated capability module for reading, writing, and refactoring code on request. Sub-modules: `analyzers/` (static analysis / code understanding), `generators/` (code generation/editing).
