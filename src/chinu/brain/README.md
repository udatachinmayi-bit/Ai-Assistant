# AI Brain

Understands the user. Converts transcribed text + context into a structured, actionable intent. Sub-modules: `llm/` (model backend adapters), `intent/` (intent parsing), `context/` (context window building), `reasoning/` (planning and multi-step decisions).
