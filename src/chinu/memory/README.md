# Memory System

Gives Chinu memory across turns and sessions. Sub-modules: `short_term/` (in-session context), `long_term/` (durable facts/preferences via the database layer), `vector_store/` (semantic/embedding-based recall).
