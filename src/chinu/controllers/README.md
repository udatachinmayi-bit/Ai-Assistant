# System Controllers

The assistant's "hands." Each controller is isolated so it can be replaced or disabled independently. Sub-modules: `windows_controller/` (OS-level control), `browser_controller/` (browser automation), `file_controller/` (filesystem operations).
