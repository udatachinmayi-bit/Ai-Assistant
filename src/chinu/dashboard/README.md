# Dashboard

Local UI for observing and controlling Chinu (conversation history, plugin status, logs, settings). Split into `backend/` (API) and `frontend/` (UI) so the presentation layer can change independently of assistant logic.
