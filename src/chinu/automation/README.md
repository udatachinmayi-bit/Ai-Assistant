# Automation Engine

Executes resolved intents safely. Sub-modules: `tasks/` (atomic actions), `scheduler/` (time/event-triggered tasks), `workflows/` (multi-step task chains). This is where the future permission/policy gate for safe automation will live.
