# Scripts

Developer-only utility scripts — not shipped to end users and not part of the `chinu` package.

Planned contents (not yet implemented):
- `setup_env.ps1` — one-shot PowerShell script to create the virtualenv and install dependencies on Windows.
- Model download helpers, lint/format runner shortcuts, etc., added as needed.
